{
  "name": "blog-trpc-next-nest",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel server frontend",
    "build:server": "yarn --cwd packages/server build",
    "build:frontend": "yarn --cwd packages/frontend build",
    "server": "yarn --cwd packages/server dev",
    "frontend": "yarn --cwd packages/frontend dev",
    "db:studio": "yarn --cwd packages/server db:studio",
    "db:push": "yarn --cwd packages/server db:push",
    "db:pull": "yarn --cwd packages/server db:pull"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}
